/**
 * @fileoverview Single source of truth for the runtime version string.
 * Bump it together with package.json and
 * plugins/jira-tools/.claude-plugin/plugin.json on every release.
 */
export const VERSION = '0.5.3'

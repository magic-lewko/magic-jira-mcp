# Dependencies
node_modules/

# Config / secrets — token must never reach the repo
.env
.env.*
.env.local
*.local
config.json
jira-tools.config.json

# Claude Code local session settings (never shared)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
